import React from "react";
var FormikStep = function (_a) {
    var children = _a.children, style = _a.style;
    return (React.createElement("div", { style: style }, React.Children.toArray(children).map(function (child, index) {
        if (!React.isValidElement(child))
            return null;
        return React.cloneElement(child, { key: "formikStep-key-".concat(index) });
    })));
};
export default React.memo(FormikStep);
