import React from "react";
import { SelectFieldProps } from "../../types";
export declare const SelectField: React.MemoExoticComponent<({ label, value, name, options, placeholder, readOnly, className, labelColor, component, ...props }: SelectFieldProps) => JSX.Element>;
export default SelectField;
