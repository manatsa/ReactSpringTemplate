import React from "react";
import { InputFieldProps } from "../../types";
interface PropTypes extends InputFieldProps {
    type: "text" | "password" | "email";
    inline?: boolean;
    floating?: boolean;
    disabled?: boolean;
    className?: string;
    style?: React.CSSProperties;
}
export declare const InputField: React.MemoExoticComponent<({ className, inline, floating, component, label, placeholder, style, ...props }: PropTypes) => JSX.Element>;
export default InputField;
